type Counter = variant {
	topic : text;
	value : nat64;
};
type Subscriber = variant {
	topic : text;
};

type DataModel = record {
	ticker : text;
	chain_id : nat64;
	recipient_address : text;
	amount : nat64;
};

service : {
	"setup_subscribe" : (publisher_id : principal) -> ();
	"get_remittance": (text, nat64, text) -> (DataModel) query;
	"request" : () -> (text) query;
};
