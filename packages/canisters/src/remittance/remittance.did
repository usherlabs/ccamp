type Counter = variant {
	topic : text;
	value : nat64;
};
type Subscriber = variant {
	topic : text;
};

type DataModel = record {
	ticker : text;
	chain_id : text;
	chain_name : text;
	recipient_address : text;
	amount : nat64;
};

service : {
	"setup_subscribe" : (publisher_id : principal) -> ();
	"get_remittance": (text, text,text, text) -> (DataModel) query;
	"request" : () -> (text) query;
};
