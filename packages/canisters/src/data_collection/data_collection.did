type Counter = record {
	value : nat64;
};
type Subscriber = record {
	topic : text;
};

service : {
	"greet" : (text) -> (text) query;
	"subscribe" : (subscriber : Subscriber) -> ();
	"publish" : () -> ();
};
