type Counter = record {
	value : nat64;
};
type Subscriber = record {
	topic : text;
};

service : {
	"owner" : () -> (text) query;
	"name" : () -> (text) query;
	"subscribe" : (subscriber : Subscriber) -> ();
	"is_subscribed" : (canister : principal) -> (bool) query;
	"publish" : () -> ();
};
